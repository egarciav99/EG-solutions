import { ProjectCase } from '../types';
// Generado desde eg-content (projects.json → scripts/build-webs.mjs). No editar a mano:
// los cambios se hacen en eg-content y se sincronizan aquí.
import cases from './projects.json';

export const CASE_STUDIES = cases as ProjectCase[];
